/// Zakat calculation constants (no nisab threshold — gold items only).
class ZakatConstants {
  ZakatConstants._();

  /// Standard zakat rate: 2.5% (1/40).
  static const double zakatRate = 0.025;
}
